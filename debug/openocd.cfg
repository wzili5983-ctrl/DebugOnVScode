# STM32F407ZG / ST-LINK / SWD. Paths resolve through OpenOCD's scripts directory.
source [find interface/stlink.cfg]
transport select hla_swd
source [find target/stm32f4x.cfg]
adapter speed 1000
# Works without an NRST wire. Connect NRST if connect-under-reset is needed.
reset_config none
